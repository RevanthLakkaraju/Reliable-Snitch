SITE_ORIGIN=http://localhost:3000
# Hosting-only team-demo access. Generate independent, random 32-byte secrets.
# Set these as runtime secrets in Sites; never commit invitation/session secrets.
# DEMO_ACCESS_CODE_HASH=<sha256-hex-of-random-invitation-code>
# DEMO_SESSION_SECRET=<independent-random-32-byte-base64url-key>
